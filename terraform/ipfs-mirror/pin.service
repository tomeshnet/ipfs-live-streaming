[Unit]
Description=IPFS daemon
Wants=network.target
After=network.target

[Service]
Type=simple
User=root
Group=root
ExecStart=/root/pin-service.sh
ExecStop=/usr/bin/pkill -f pin-service.sh
Restart=on-failure
RestartSec=10s

[Install]
WantedBy=multi-user.target
